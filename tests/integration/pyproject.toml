[project]
name = "integration"
version = "0.1.0"
description = "HTTP integration tests for the hackmty26 detect API"
readme = "README.md"
requires-python = ">=3.11, <3.13"
dependencies = [
    "pytest>=9.1.1",
    "httpx2>=2.12",
]
